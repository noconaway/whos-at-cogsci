{

 // good to launch via terminal
 // rscript -e "library(shiny);runApp(getwd())"

	"folders":
	[
		{
			"file_exclude_patterns":
			[
				"*.sublime-workspace",
				"Rplots.pdf"
			],
			"path": "."
		}
	]
}
